<UserControl x:Class="ApptReminderWindowsClient.ScheduleInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="StartValue" SelectionChanged="StartValue_SelectionChanged" />
        <Label Grid.Column="1" Content="To" HorizontalAlignment="Center" />
        <ComboBox x:Name="EndValue" Grid.Column="2" />
    </Grid>
</UserControl>
