<Window x:Class="ApptReminderWindowsClient.AddItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title, FallbackValue='Add New'}" Height="{Binding Height, FallbackValue='100'}" Width="{Binding Width, FallbackValue='400'}" WindowStartupLocation="CenterOwner">
    <Grid IsVisibleChanged="Grid_IsVisibleChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*" />
            <ColumnDefinition Width="0.25*" />
            <ColumnDefinition Width="0.25*" />
            <ColumnDefinition Width="0.25*" />
        </Grid.ColumnDefinitions>
        <Label Content="{Binding Label, FallbackValue='Name'}" FontWeight="Bold" Margin="5 5 0 0" />
        <TextBox x:Name="Text" Grid.Column="1" Grid.ColumnSpan="3" Margin="0 5 0 0" KeyDown="Text_KeyDown" />
        <Button Content="Save" Grid.Row="1" Grid.Column="2" Margin="5" Click="Save_Click" />
        <Button Content="Cancel" Grid.Row="1" Grid.Column="3" Margin="5" Click="Cancel_Click" />
    </Grid>
</Window>
